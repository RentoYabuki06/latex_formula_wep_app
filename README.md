# LaTeX Studio

LaTeX数式をライブプレビューしながら作成し、高画質の背景透過PNG / SVG / PDFで書き出せる個人用Webアプリ。

- 数式レンダリング: MathJax v3 (SVG出力)
- エクスポート: 透過PNG (解像度指定) / SVG / PDF
- 履歴: Supabase (Postgres) に保存、どこからでもアクセス可能
- 認証: Google OAuth (Supabase Auth)
- ホスティング: Vercel

セットアップ手順は `docs/SETUP.md` を参照。
